export interface styling
  {
    color:string,
    backgroundColor:string
    padding:string
    border:string
  }

  export interface details {
    name: string;
    email: string;
    city: string;
    area: string;
    pincode:number;

  }